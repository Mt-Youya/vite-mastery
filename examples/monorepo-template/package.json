{
  "name": "monorepo-template",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @monorepo/web dev",
    "build": "turbo build",
    "typecheck": "tsc --noEmit",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.11.2",
    "typescript": "^7.0.2"
  }
}
