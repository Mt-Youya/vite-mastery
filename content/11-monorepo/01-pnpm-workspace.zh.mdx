---
title: pnpm workspace 基础
description: pnpm workspace 是 Vite monorepo 的基础——workspace 协议、依赖提升策略、Hoisting vs Isolation 的权衡。
order: 1
part: 11-monorepo
chapter: "11.1"
difficulty: 2
readingTime: 12
updatedAt: "2026-06-26"
viteVersion: "8.1"
apiStability: "stable"
---

<Callout type="note" title="你将学到什么">
  - `pnpm-workspace.yaml` 的配置方式 - `workspace:*` 协议:本地包引用 - pnpm 的依赖提升策略(Hoisting) - `.npmrc`
  关键配置项
</Callout>

<Callout type="tip" title="前置知识">
  - 了解 npm 包依赖的基本概念 - 知道什么是 monorepo(多包仓库)
</Callout>

## pnpm-workspace.yaml

声明 monorepo 中哪些目录是 workspace packages:

```yaml filename="pnpm-workspace.yaml"
packages:
  - "apps/*" # apps/ 下所有目录
  - "packages/*" # packages/ 下所有目录
  - "examples/*" # examples/ 下所有目录
  - "!**/node_modules/**" # 排除 node_modules
```

Glob 语法:

- `apps/*`:apps/ 下一层的所有目录
- `packages/**`:packages/ 下所有层级
- `!pattern`:排除匹配的目录

## `workspace:*` 协议

本地包之间的相互引用使用 `workspace:*` 协议:

```json filename="apps/web/package.json"
{
  "dependencies": {
    "@vite-mastery/ui": "workspace:*",
    "@vite-mastery/mdx-components": "workspace:*"
  }
}
```

`workspace:*` 的含义:

- **开发时**:链接到本地的 `packages/ui/` 目录(软链接)
- **发布时**:替换为实际的版本号(如果要发布到 npm)
- **不用发布时**:保持 `workspace:*` 即可

## 常用 pnpm workspace 命令

```bash
# 在根目录操作所有 workspace
pnpm -r run build       # 递归运行所有包的 build
pnpm -r run typecheck   # 递归运行 typecheck

# 只操作特定包
pnpm --filter @vite-mastery/web dev
pnpm --filter ./packages/ui build

# 在某个包里安装依赖
pnpm add react --filter @vite-mastery/web
pnpm add -D typescript --filter @vite-mastery/ui

# 安装所有包的依赖
pnpm install
```

## .npmrc 关键配置

```ini filename=".npmrc"
# 不提升 node_modules 到根目录(更严格的隔离)
shamefully-hoist=false

# 自动安装 peer dependencies
auto-install-peers=true

# 严格模式:package.json 里没声明的依赖不能使用
# (防止 phantom dependencies)
strict-peer-dependencies=false
```

## 依赖提升(Hoisting)的权衡

pnpm 默认**不提升**依赖到根 `node_modules`:

```text
# Hoisting 关闭(pnpm 默认):
apps/web/node_modules/react     ← web 独立的 react
packages/ui/node_modules/react  ← ui 独立的 react
# 每个包有自己的 node_modules,更隔离

# Hoisting 开启(shamefully-hoist=true):
node_modules/react              ← 所有包共用一个 react
# 节省空间,但有幽灵依赖风险
```

**推荐**:保持 `shamefully-hoist=false`(pnpm 默认),使用 `auto-install-peers=true` 处理 peer deps。

## Vite 项目里的 workspace 关键点

Vite monorepo 和普通 Node monorepo 的差异在于:源码包会直接进入 dev server。也就是说,`packages/ui/src/Button.tsx` 可能被 `apps/web` 直接 import,再由 Vite 转换和 HMR。

这带来几个规则:

1. 本地包要在 `package.json` 里声明清楚 `exports`,不要只靠路径 alias。
2. React、Vue 这类框架运行时应该放到 `peerDependencies`,避免多个副本。
3. 如果应用直接消费本地包源码,要确认 Vite 能处理该包的 TS/JSX/CSS。
4. 如果本地包先 build 再消费,要确认 dist、types 和 exports 三者一致。

最常见的问题是“一份 React 变成两份”。如果 `apps/web` 和 `packages/ui` 各自装了不同 React 实例,HMR、context、hooks 都可能出错。解决方向不是打开 hoist 硬压平,而是把 React 放进组件库的 `peerDependencies`,由应用提供唯一运行时。

## filter 命令怎么读

`pnpm --filter` 是 monorepo 日常效率的关键:

```bash
pnpm --filter @my/web dev
pnpm --filter @my/ui build
pnpm --filter "...@my/web" build
pnpm --filter "@my/ui..." test
```

常用语义:

| 写法 | 含义 |
| --- | --- |
| `@my/web` | 只选中这个包 |
| `...@my/web` | 选中 `@my/web` 以及它依赖的包 |
| `@my/ui...` | 选中 `@my/ui` 以及依赖它的包 |
| `./packages/ui` | 通过路径选中包 |

会用 filter 以后,你不必每次都 `pnpm -r run build` 扫全仓库。对于大型 monorepo,这会直接影响日常反馈速度。

## 自测题

1. `workspace:*` 和 `workspace:^1.0.0` 有什么区别?
2. `shamefully-hoist=false` 对项目有什么影响?为什么叫 "shamefully"?
3. 用 `pnpm -r run build` 和用 Turborepo 的 `turbo build` 有什么区别?
4. 如果 `packages/ui` 需要用 `packages/utils` 里的工具函数,应该怎么配置?

```yaml filename="练习"
# 配置一个 monorepo 的 pnpm-workspace.yaml:
# 结构:
# - apps/web (Next.js 主站)
# - apps/docs (文档站)
# - packages/design-tokens (设计 token)
# - packages/ui (组件库)
# - packages/hooks (React hooks)
# - packages/utils (工具函数)
# - examples/ 下有多个独立示例,不需要加入 workspace

# 写出 pnpm-workspace.yaml 和 .npmrc 的内容
```
