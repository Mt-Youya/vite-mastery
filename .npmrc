# 不把依赖提升到根,避免幽灵依赖
shamefully-hoist=false
# 自动安装 peerDependencies
auto-install-peers=true
# peer 不严格,workspace 内部包互引时更灵活
strict-peer-dependencies=false
# 锁定 lockfile 版本
lockfile=true
# 加快冷启动
prefer-frozen-lockfile=false
